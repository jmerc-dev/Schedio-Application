<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="Button" x:Key="HomeNavButton">
        <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="40,0,40,0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">

                    <Border x:Name="Border" CornerRadius="5" BorderThickness="5" MinHeight="80" MinWidth="100" BorderBrush="{StaticResource SecondaryColor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                        <Grid>
                            <Rectangle x:Name="Rectangle" Fill="{StaticResource SecondaryColor}"/>
                            <Grid MinHeight="80" MinWidth="100" x:Name="Panel" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="3*"/>
                                    <RowDefinition Height="2*"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Rectangle" Property="Fill" Value="{StaticResource ShadedSecondaryColor}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource ShadedSecondaryColor}"/>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ShadedSecondaryColor}"/>  
                            
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>


            </Setter.Value>
        </Setter>

    </Style>
    
</ResourceDictionary>